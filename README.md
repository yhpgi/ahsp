# Data Referensi AHSP

Repository ini berisi data referensi AHSP yang telah dinormalisasi.

Data ditulis dalam format JSONL bershard dengan manifest dan checksum pada struktur `data/schema-v1/latest/`.
